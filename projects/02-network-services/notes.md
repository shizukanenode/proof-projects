# Notes

- The VM hostname was `lab-srv01`.
- The VM IP used for testing was `192.168.64.2`.
- SSH was already active when checked.
- Nginx installed successfully and started without errors.
- Ports 22 and 80 were listening on the VM.
- Local HTTP test returned `200 OK`.
- Remote tests from the Mac reached both port 22 and port 80.
- Ubuntu firewall (`ufw`) was inactive.
- I accidentally ran some Linux commands on the Mac, which caused command errors.
- I also initially started the terminal recording from the wrong location, then corrected it and saved the transcript in the proper evidence folder.
